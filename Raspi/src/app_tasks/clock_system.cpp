#include<ctime>
//時間系のシステムを定義するため。

#include"clock_system.hpp"

class clock_system
{
private:
    /* data */
public:
    clock_system(/* args */);
    ~clock_system();
};

clock_system::clock_system(/* args */)
{
}

clock_system::~clock_system()
{
}
