ECC_root{
    docs{
        branch_data.txt(ブランチの構成メモ)
        diary.txt(メモ用)
        file_data_template.txt(ファイル構成の例文的な)
        file_data.txt(実際のファイルの構成メモ)
    }   
    Raspi{
        build{
            //cmakeのビルド用ディレクトリ
        }
		scr{
            clock_system.cpp(clock_system.hppの実体)
		}
        inc{
            clock_system.hpp(時計、アラームに関するコードヘッダーファイル)
        }
		lib{
				
		}
		config{
			hardconfig.h(基本定数,基本変数)
            
		}
        driver{
            
        }
        main.cpp(システムメインコード)
    }
}